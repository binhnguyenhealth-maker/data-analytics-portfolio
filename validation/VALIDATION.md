# Public Portfolio Release Validation

**Verdict:** `PASS_PUBLIC_REPOSITORY_PUBLISHED`

- Independent verification by: Codex
- Verification date: 2026-07-20 (America/New_York)
- Release surface: source, tests, documentation, three packaged Tableau
  workbooks, and three source-backed presentation previews
- External state at this checkpoint: public repository published and pinned at
  `https://github.com/binhnguyenhealth-maker/data-analytics-portfolio`

The original GLM 5.2 staging validation is preserved unchanged at
`validation/history/GLM52_STAGING_VALIDATION_2026-07-17.md` with its original
authorship and execution-surface labels.

## Exact release checks

```sh
PYTHONDONTWRITEBYTECODE=1 python3 -B shared/validation/regenerate_all.py
PYTHONDONTWRITEBYTECODE=1 python3 -B shared/validation/build_manifest.py
PYTHONDONTWRITEBYTECODE=1 python3 -B shared/validation/run_all_tests.py
```

Latest result: **169 tests passed, 0 failed**.

The same suite is configured for every GitHub push and pull request and can be
run manually from the repository's Actions page.

The suite includes:

- 128 project-level analytical and mutation tests;
- disclosure, entity-provenance, structure, and determinism controls;
- fresh-process regeneration under `PYTHONHASHSEED` values 1, 2, and 7;
- exact Tableau package inventory and XML checks;
- byte equality between every workbook-embedded CSV and its freshly generated
  canonical counterpart;
- fixed 1366 x 768 dashboard checks;
- exact worksheet and dashboard inventories;
- presentation-preview format and dimension checks; and
- negative cases for private-path and real-entity contamination.

## Tableau evidence

All three workbooks were opened, rendered, saved, closed, and freshly reopened
in Tableau Desktop 2026.2 Free Edition before promotion into this release
payload. The repository landing-page images are clean presentation views
generated from the same embedded synthetic tables; they are not represented as
native screenshots.

| Project | Native readback | Packaged workbook |
|---|---|---|
| Data Quality Command Center | 27 inventory, 6 queue, 25 lineage, 1 remediation row | `projects/data-quality-command-center/tableau/` |
| Urban Mobility Gap Diagnostic | 24 scatter marks from 24 synthetic score rows | `projects/urban-mobility-gap/tableau/` |
| Peer Scenario Explorer | default focal city and baseline scenario; four surfaces present | `projects/peer-scenario-explorer/tableau/` |

Tableau Optimizer was visible but disabled in the Free Edition. A Performance
Recording control produced 133 events but no query-bearing interaction inside
the bounded recording window, so it is retained only as proof that the control
ran and is not described as query-performance evidence.

## Disclosure and rights

- Every packaged source row is synthetic and generated by this repository.
- The release scan found no private local path, real source-organization name,
  private contact, or access material in the public payload.
- The workbook package XML uses relative `Data/synthetic` paths.
- The repository is licensed under Apache License 2.0.
- Tableau product naming is used only to describe compatible workbook files;
  no affiliation or endorsement is implied.

## Known limitations

- These are independent prototypes, not paid engagements or accepted client
  deliverables.
- Hosted Tableau administration, row-level security, production refresh,
  training, and measured business outcomes are outside the demonstrated scope.
- The previews are static presentation views; download the packaged workbooks
  to inspect the native Tableau implementation and controls.

## Manifest

`validation/INCLUDED_FILES_SHA256.md` is the non-circular deterministic
SHA-256 inventory of the exact version-controlled payload. It is regenerated
only after all other tracked bytes are stable and is verified by the structure
test.
